DATABASE_URL="postgresql://user:password@localhost:5432/buildcrm?schema=public"
JWT_SECRET="your-secret-key"
PORT=4000
CORS_ORIGIN="http://localhost:3000"
# Для загруженных файлов (WhatsApp): публичный URL API, напр. https://your-api.onrender.com
API_URL="http://localhost:4000"

# Cloudinary (бесплатно) — медиа хранятся в облаке, ссылки не 404 на Render
# Без этих переменных — fallback на локальный uploads (на Render эфемерный диск → 404)
# cloudinary.com → Dashboard → API Keys
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=