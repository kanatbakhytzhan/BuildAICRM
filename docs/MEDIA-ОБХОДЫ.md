# Варианты обхода: как доставить голос и фото в WhatsApp, если ChatFlow не шлёт медиа

ChatFlow не имеет рабочего send-media API, а узлы «Аудио»/«Изображение» в конструкторе не отправляют по URL из вебхука. Ниже — **все разумные варианты** обхода.

---

## 1. Оставить как есть (ссылки текстом)

**Что происходит сейчас:** мы шлём в чат сообщения вида «🎵 Голосовое: https://...» и «🖼 Фото: https://...». Клиент жмёт ссылку и открывает в браузере.

**Плюсы:** уже работает, ничего не надо менять.  
**Минусы:** не нативное медиа в чате, хуже восприятие.

---

## 2. Одна «красивая» ссылка вместо двух

**Идея:** не слать две отдельные ссылки (голос + фото), а сделать одну страницу на нашем API, например:

`https://buildcrm-api.onrender.com/p/welcome/<leadId>`

На странице: плеер для голоса + картинки из приветствия. В WhatsApp отправить один раз текст: «Ваше приветствие: [открыть]» с этой ссылкой.

**Плюсы:** один клик, всё собрано на одной странице.  
**Минусы:** по-прежнему не голос/фото внутри WhatsApp.

**Что нужно:** добавить в API простой HTML-роут (или статику), который по leadId подставляет нужные URL голоса и фото из темы/лида.

---

## 3. Официальный WhatsApp Business API (Meta)

**Идея:** отправлять медиа через официальный API Meta (WhatsApp Cloud API). У него есть отправка медиа по URL (image, audio, document).

**Плюсы:** нативные голос и фото в чате, стабильно и по правилам.  
**Минусы:** нужен Facebook Business, верификация, номер переводится на Meta; есть квоты и возможная плата за сообщения.

**Что нужно:** в BuildCRM добавить интеграцию с WhatsApp Cloud API (отдельный канал или замена ChatFlow для исходящих). Входящие можно оставить в ChatFlow и дублировать в CRM, а исходящие с медиа — слать через Meta.

---

## 4. Другой провайдер с send-media (Twilio, MessageBird, и т.д.)

**Идея:** использовать сервис, у которого есть нормальный API отправки медиа в WhatsApp (по URL).

**Плюсы:** нативное медиа, часто проще, чем Meta.  
**Минусы:** нужен аккаунт у провайдера, номер или маршрутизация через них.

**Что нужно:** в настройках тенанта добавить тип канала «Twilio»/«MessageBird»/другой, в MessagesService вызывать их API для отправки медиа по URL вместо ChatFlow send-media.

---

## 5. n8n / Flowise между ChatFlow и BuildCRM

ChatFlow как раз и предлагает подключить WhatsApp к **n8n** или **Flowise**. Кратко, что они дают и почему медиа всё равно упирается в ChatFlow.

### n8n

- **Что это:** оркестратор сценариев (воркфлоу): триггеры, HTTP, условия, интеграции.
- **С ChatFlow:** входящее из WhatsApp приходит в n8n как вебхук от ChatFlow. В n8n можно вызвать наш API (получить `reply`, `welcomeVoiceUrl`, `welcomeImageUrls`), потом отправить ответ обратно.
- **Отправка в WhatsApp:** идёт через тот же канал, что даёт ChatFlow. То есть n8n дергает **API ChatFlow** (send-text / send-media). У нас уже проверено: **send-text работает, send-media отдаёт HTML** и не реализован. n8n это не обходит — он лишь ещё один клиент того же API.
- **Вывод:** n8n удобен, чтобы собрать логику (наш вебхук → ответ → решение, что слать). Но **медиа по-прежнему не отправятся**, пока ChatFlow не сделает рабочий send-media или способ «отправить по URL» в своём API/конструкторе.

### Flowise

- **Что это:** низко‑кодовый конструктор под AI-чаты (LangChain, RAG, чат-интерфейс).
- **С ChatFlow:** WhatsApp подключается к Flowise через ChatFlow; чат ведётся уже в Flowise (промпты, цепочки, память).
- **Медиа:** в Flowise есть загрузки (uploads) и вложения, но отправка **именно в WhatsApp** снова идёт через подключение ChatFlow. Если у ChatFlow нет отправки медиа по URL, Flowise не добавит эту возможность сам — он только использует тот же канал.

### Итог по n8n и Flowise

| Вопрос | Ответ |
|--------|--------|
| Можно ли внедрить n8n/Flowise вместо/рядом с текущим вебхуком BuildCRM? | Да: входящие из WhatsApp можно пустить в n8n/Flowise, оттуда вызывать наш API. |
| Решат ли они отправку голоса/фото в WhatsApp? | **Нет**, если отправка идёт через ChatFlow: у них один и тот же API, send-media не работает. |
| Когда n8n/Flowise помогут с медиа? | Когда у ChatFlow появится рабочий send-media или способ отправить медиа по URL; тогда в n8n можно будет добавить шаг «HTTP Request → send-media» и он заработает. |

**Практично:** n8n имеет смысл, если хочешь всю логику (AI, ветвления, другие сервисы) держать в воркфлоу и одним запросом к нашему API забирать `reply` + URL медиа. Для **нативного** голоса/фото в чате всё равно нужно либо дополнение API со стороны ChatFlow, либо другой канал (WhatsApp Business API, другой провайдер) — см. варианты 3 и 4 выше.

---

## 6. Свой коннектор к WhatsApp (Baileys, whatsapp-web.js и т.п.)

**Идея:** поднять отдельный сервис (например на базе Baileys), который подключается к WhatsApp по QR, и из нашего API вызывать его «отправить медиа по URL».

**Плюсы:** полный контроль, можно слать что угодно.  
**Минусы:** против ToS WhatsApp для бизнес-использования, нужен стабильный процесс, повторная авторизация при перезапуске, риски бана номера.

**Что нужно:** отдельный микросервис + в BuildCRM опция «канал: Baileys» и вызов его API для отправки медиа.

---

## 7. Давление на ChatFlow (поддержка / запрос фичи)

**Идея:** писать в поддержку ChatFlow: «Нужен API send-media (GET/POST с token, instance_id, jid, url, type) или в конструкторе — отправка аудио/изображения по URL из ответа вебхука».

**Плюсы:** если добавят — всё решится без смены провайдера.  
**Минусы:** от них ничего не зависит от нас, только ждать.

---

## 8. Два канала: текст через ChatFlow, медиа через другую систему

**Идея:** текст по-прежнему через ChatFlow, а голос/фото слать, например, в Telegram (если у клиента есть) или по SMS/email ссылкой. Для WhatsApp оставить только текст.

**Плюсы:** можно реализовать быстро (например Telegram Bot API по URL).  
**Минусы:** медиа не в том же чате WhatsApp, другой канал для пользователя.

---

## Сводка

| Вариант                         | Нативное медиа в WhatsApp? | Сложность | Зависит от нас? |
|---------------------------------|----------------------------|-----------|------------------|
| 1. Ссылки текстом               | Нет                        | —         | Да               |
| 2. Одна страница «приветствие»  | Нет                        | Низкая    | Да               |
| 3. WhatsApp Business API (Meta)| Да                         | Высокая   | Да (интеграция)  |
| 4. Другой провайдер (Twilio и т.д.) | Да                     | Средняя   | Да (интеграция)  |
| 5. n8n/Flowise                  | Зависит от нод             | Средняя   | Частично         |
| 6. Baileys/свой коннектор       | Да                         | Высокая   | Да (риск ToS)    |
| 7. Запрос фичи в ChatFlow       | Да, если сделают           | Нулевая   | Нет              |
| 8. Медиа в Telegram/другой канал| В другом мессенджере       | Средняя   | Да               |

Реалистичные следующие шаги: **2** (быстро улучшить UX ссылок), **3** или **4** (если нужны именно нативные голос/фото в WhatsApp), **7** (параллельно писать в ChatFlow).
