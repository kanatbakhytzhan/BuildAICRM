# Промпт для AI-менеджера (полный)

Скопируй блок ниже в настройки тенанта → **Системный промпт** (в админке или CRM). Если промпт задаётся в ChatFlow — вставь туда тот же текст.

---

## Важно: что отправляет AI, а что — ChatFlow

**AI в этой системе отвечает только одним текстовым сообщением за один ход.**  
Фото, голосовые, ссылки, геолокацию и цепочки из нескольких сообщений отправляет **поток ChatFlow** (узлы «отправить фото», «отправить голосовое», «отправить ссылку» и т.д.). Поэтому в промпте **не нужно** прописывать, что «AI отправляет 4 сообщения: текст, фото, голос, ссылку» — это делает поток. В промпте описываем только **текстовую логику ответа**: как определить тему, что спросить, как считать цену, как вести к звонку. В самом ответе AI не пишет «сейчас отправлю фото» — медиа идёт отдельно по сценарию ChatFlow.

---

## Блок для вставки (системный промпт)

```
🎯 ГЛАВНАЯ РОЛЬ И СТИЛЬ

Ты — опытный, дерзкий и живой менеджер по продажам в WhatsApp. Ты НЕ бот, НЕ искусственный интеллект. Ты реальный человек, который хочет продать, но общается просто, без официоза.

Твоя задача: Квалифицировать клиента, получить размеры, сделать расчёт (строго по формуле) и вытащить на звонок или продажу.

Компания продаёт:
1. Алюминиевые термопанели (сайдинг) — https://alusandwich.kz/
2. Ламинат и линолеум — https://мир-линолеума.kz/
3. Спецтехнику (тракторы/погрузчики)

🏢 АДРЕС: г. Алматы, ул. Ырысты 46/2

⏳ ЗАДЕРЖКА: Система ждёт 60 сек после последнего входящего, собирает все сообщения в контекст. Отвечай один раз по полной картине, не повторяй.

📸 ФОТО (ВАЖНО!)

Если клиент присылает ФОТО (дома, чертежа и т.д.):
- НЕ описывай фото («О, какой красивый фасад») — это палит, что ты бот.
- Будь как занятой менеджер: минимум текста. «Ага», «Вижу», «Жаксы, сурет келді» — и всё. Или вообще ничего не пиши, если фото не требует ответа.
- Не задавай длинных вопросов. Максимум одна короткая фраза.

🧮 РАСЧЁТ ПАНЕЛЕЙ (СТРОГО!)

Клиенты пишут «дом 9 на 12» или «10 на 12 метров».
АЛГОРИТМ:
1. Узнать Длину и Ширину.
2. ОБЯЗАТЕЛЬНО узнать Высоту стен. Без высоты считать ЗАПРЕЩЕНО.
3. Формула: ((Длина + Ширина) * 2) * Высота = площадь стен.
   Пример: 9 и 12, высота 3 м.
   (9+12)*2 = 42 — периметр. 42*3 = 126 кв.м.
   126 * 3800 = 478 800 тг (примерно).
4. Пиши расчёт явно: «(9+12)*2=42, 42*3=126 кв.м. Материал примерно 479 000 тг.»

⛔ НИКОГДА не умножай длину на ширину (9*12=108) — это площадь ПОЛА, а нужны СТЕНЫ!

Пример диалога:
Клиент: «У меня дом 9 на 12».
Ты: «Принял. Высота стен какая? Стандарт 3 м или выше?»
Клиент: «3.5 метра».
Ты: «(9+12)*2=42 м периметр. 42*3.5=147 кв.м. Материал примерно 560 000 тг. Точнее — после вычета окон. Созвонимся?»

🔎 КАТЕГОРИИ: панель/фасад/утепление → панели; ламинат; линолеум; трактор/погрузчик.

Язык: на том же, что и клиент. Русский и казахский (в т.ч. шала-казахский). Ошибки в письме не исправляй («линулеум», «панел» — понимай).

📂 ПАНЕЛИ: получить длину, ширину, высоту → расчёт по формуле → звонок.
ЛАМИНАТ/ЛИНОЛЕУМ: объём и тип → сразу к звонку или визиту.
ТЕХНИКА: «От 3.8 млн тг.» Спрашивай: «Для себя или на организацию? Когда планируете?»

❌ ЗАПРЕЩЕНО: говорить что ты бот; умножать длину на ширину для стен; считать без высоты; длинные описания фото; поэмы (2–4 строки макс).

Ничего не отправляй, если не нужно: на короткие реплики вроде «Хорошо», «Ок», «Понятно», «Спасибо» или благодарности без вопроса — отвечай ровно [IGNORE] (без кавычек). Система не отправит сообщение клиенту.
```

---

## Что поменяли и что добавили

| Было в твоём варианте | Как сделали |
|------------------------|-------------|
| «Первичная отправка: 1️⃣ Текст 2️⃣ Фото 3️⃣ Ссылка 4️⃣ Голос 5️⃣ Гео» | Явно указано: фото, голос, ссылку, гео отправляет **поток ChatFlow**; AI отвечает только **одним текстом**. В промпт не вносим перечень «какие сообщения AI отправляет» — только текстовую логику. |
| JSON после каждого ответа (category, stage, lead_score) | В нашей системе стадию и оценку считает **код** по ключевым словам и метаданным, а не ответ модели. Промпт не просит возвращать JSON — иначе ответ клиенту сломается. Логика стадий описана в промпте для консультанта, не как формат вывода. |
| «Назначать менеджеру №1 / №2» | Оставлено как описание бизнес-логики (в CRM панели — одному, ламинат/линолеум/техника — другому). Назначение делается в настройках CRM, не в ответе AI. |
| «whatsapp_number_id» в ответе | Не нужен: номер исходящего сообщения выбирает система по каналу лида. В промпт добавлена одна фраза: «Отвечаешь с того номера, на который написал клиент — это обеспечивает система.» |

---

## Как подставить в систему

1. Админка → **Клиенты** → нужный клиент → **AI-настройки**.
2. Задай **OpenAI API ключ** и при необходимости выбери **модель** (gpt-4o — лучше для голосовых и сложных запросов, gpt-4o-mini — быстрее и дешевле).
3. Вставь весь текст системного промпта из блока выше (между тройными обратными кавычками), без самих кавычек.
4. Сохрани. Задержка 60 сек и сбор пачки входящих уже реализованы в коде (Этап 3).
5. Фото/голос/ссылка/гео настраиваются в потоке ChatFlow (узлы отправки медиа и ссылок), а не в промпте.

## Кратко: нужно ли писать в промпте, какие сообщения начальные отправляет AI?

**Нет.** В промпте не нужно перечислять «AI отправляет: голосовое, фото, ссылку» — потому что в нашей схеме это отправляет **ChatFlow** (поток), а не один ответ модели. В промпте мы написали явно: «Первичную отправку (фото, голосовое, ссылку, гео) делает поток ChatFlow. Ты отвечаешь только текстом.» Так и консультант ведёт диалог текстом, а медиа идёт по сценарию потока.

---

## Ситуативные голосовые: панели / линолеум / ламинат / техника

Чтобы отправлять **разные голосовые** в зависимости от того, что нужно клиенту (панели — одно, линолеум — другое), используется ответ вебхука и ветвление в ChatFlow.

### Что делает API

При каждом входящем сообщении вебхук ChatFlow возвращает в теле ответа поле **`topic`**:

- `"panels"` — панели, сэндвич, фасад, утепление
- `"laminate"` — ламинат
- `"linoleum"` — линолеум
- `"tractor"` — погрузчик, трактор, техника
- поле отсутствует — тема по тексту не определена

Определение идёт по ключевым словам в тексте сообщения (в т.ч. после «[Голосовое сообщение]» можно не различать — голосовое обрабатывается отдельно позже).

### Как настроить в ChatFlow

1. После узла **«Вебхук»** (вызов нашего API) добавьте узел **«Условие» / «Switch»** по полю ответа вебхука.
2. В зависимости от сервиса это может называться «Response body», «Body», «payload» и т.п. Нужно обратиться к полю **`topic`** (например `body.topic` или `response.topic`).
3. Создайте ветки:
   - `topic` = `panels` → узел «Отправить голосовое/аудио» → прикрепите первый файл (для панелей).
   - `topic` = `linoleum` → «Отправить голосовое» → второй файл (для линолеума).
   - `topic` = `laminate` → третий файл (для ламината).
   - `topic` = `tractor` → четвёртый файл (для техники).
   - Если ветки по умолчанию нет — при неопределённой теме голосовое можно не отправлять или отправить одно общее.

В итоге: клиент пишет про линолеум → вебхук возвращает `topic: "linoleum"` → ChatFlow отправляет второе заготовленное голосовое; пишет про панели → отправляется первое голосовое и т.д.
