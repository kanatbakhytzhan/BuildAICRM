Скопируй эти команды в Render Dashboard (Build failed P1001).

В Render: твой сервис → Settings → Build & Deploy:

  Build Command (без обращения к БД):
  npm ci --include=dev && npx prisma generate && npx nest build

  Start Command (миграции при старте):
  npx prisma migrate deploy && npm start

  Root Directory: apps/api

НЕ добавляй в Build Command: prisma db push или prisma migrate deploy — из-за этого P1001.

(Обновлено для синхронизации.)
